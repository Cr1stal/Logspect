# LogMan

LogMan is a simple tool that makes it easier to read the log file of a Rails application.

## Features

- Monitor the log file of a Rails application
- Display the log in a web interface
- Display the log in a web interface

## Installation

1. Clone the repository
2. Run `pnpm install`
3. Run `pnpm start`
